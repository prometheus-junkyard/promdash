angular.module("Prometheus.directives").directive('pie', function() {
  return {
    scope: {
      graph: "=graphSettings",
      servers: "=",
      aspectRatio: "=",
      index: "=",
      vars: "="
    },
    restrict: "AE",
    templateUrl: "<%= asset_path('pie_template.html.erb') %>"
  };
});
